# -Prompt-Similarity-and-Deduplication-Service
A production-ready FastAPI service for semantic search, prompt similarity detection, and duplicate clustering in large prompt libraries. It uses embeddings to understand prompt meaning and helps reduce prompt sprawl across org, team, engine, and directive layers.
